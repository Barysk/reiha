| This is an example syntax
|   - every line that starts with | is considered a comment
|     and only being seen in terminal during presentation
|   - there can be your notes
|   - the first comment block is omitted

レイハ
| The name of the tool
| inspired by sent, wend and takahashi method

Czy literówki polskie będą działać
żźćšśčŭфадвдар

@./test/screen.png
| To make image slide, start the line with @
|   Blah comment

@./test/th.png
Am image and text slide
| To make image&text slide, start the line with @ provide path to image
| and then write your text under @
|   Blah comment

| this is comment for you, and it is totally omitted

It scales
  text that you
    write to fit the
  slide
|
| Automatically
|  Of course



| Number of spaces can be any, but there must be one at least


```
...
use crate::theming::*;
use crate::utils::*;

async fn main() {
    let config = Config::from_file();

    let mut theme = config.theme.unwrap_or(DARK_THEME);
    let mut filtering = config.filtering.unwrap_or(FilterMode::Nearest);
    let mut font: Font = if let Some(path) = &config.font_path {
        let data = std::fs::read(path).expect("Failed to read font file from config");
        load_ttf_font_from_bytes(&data).expect("Failed to load font from config")
    } else {
        load_ttf_font_from_bytes(DEFAULT_FONT).unwrap()
    };
...

```
this text must be lost
|this is a code slide, starts and ends with ```


this slide
~
with empty line
| notice the ~ symbol on empty line
| it is not going to be rendered


\
| Use \ to create an empty slide

    reiha <filename>
one slide per paragraph

questions?
